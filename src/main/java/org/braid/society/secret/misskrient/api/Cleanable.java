package org.braid.society.secret.misskrient.api;

/**
 * A cleanable object that can be cleaned up.
 */
public interface Cleanable {

  /**
   * the method that will be called to Clean up the object.
   */
  void cleanUp();
}
